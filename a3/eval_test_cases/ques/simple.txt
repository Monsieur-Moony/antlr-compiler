class Program {
    int a, b, c;
    int fun ( )
    {
        a = a + b + c;
        a = b - -c;
        a = a + b * c;
        
        return (a); 

    }
    int main(int foo, boolean bar) {
        callout("printf", "a = %d\n", fun());
    }
}